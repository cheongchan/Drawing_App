<nav>
<div id="logo_type">
      <%= link_to("Drawing IO", root_path) %>
</div>
</nav>

<div id="permanent-collection">Permanent Collection</div>

<div class="grid grid-path">
    <% @images.each do |image| %>
      <div class="col-2-3">
        <%= image.svg.html_safe %>
        <div class='inner-title'>
          <%= image.title %>
        </div>
      </div>
      <% end %>
</div>
